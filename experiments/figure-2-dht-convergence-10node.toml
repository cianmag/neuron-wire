# Neuron Wire reproducible experiment — flat SimulationConfig schema.
# Run: cargo run --release --example simulate -- --config experiments/figure-2-dht-convergence-10node.toml --output-dir results/figure-2-dht-convergence-10node

description = "figure-2-dht-convergence-10node"
node_count = 10
duration_secs = 30
seed = 42
paper_mode = true
bind_prefix = "127.0.0.1"
tick_interval_ms = 1
gossip_interval_ticks = 500
cleanup_interval_ticks = 1000
max_retries = 3
gradient_half_life_ms = 100
maintenance_mode = "fixed"

