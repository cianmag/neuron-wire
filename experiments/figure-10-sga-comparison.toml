# Neuron Wire reproducible experiment — flat SimulationConfig schema.
# Run: cargo run --release --example simulate -- --config experiments/figure-10-sga-comparison.toml --output-dir results/figure-10-sga-comparison

description = "figure-10-sga-comparison"
node_count = 25
duration_secs = 30
seed = 42
paper_mode = true
tick_interval_ms = 1
gossip_interval_ticks = 500
cleanup_interval_ticks = 1000
max_retries = 3
gradient_half_life_ms = 100
maintenance_mode = "sparse-aging"

