# Neuron Wire reproducible experiment — flat SimulationConfig schema.
# Run: cargo run --release --example simulate -- --config experiments/figure-5-bandwidth-vs-nodes.toml --output-dir results/figure-5-bandwidth-vs-nodes

description = "figure-5-bandwidth-vs-nodes"
node_count = 100
duration_secs = 60
seed = 42
paper_mode = true
tick_interval_ms = 1
gossip_interval_ticks = 500
cleanup_interval_ticks = 1000
max_retries = 3
gradient_half_life_ms = 100
maintenance_mode = "fixed"

